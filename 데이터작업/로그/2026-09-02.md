# 2026-09-02

## 한 일

**DB 접속 경로 확보**
- SSH 키를 `~/.ssh/pullim_kimjuhyeon`로 설치 (OneDrive 밖으로 — 개인키가 클라우드 동기화되면 안 됨)
- `pullim@hajin.xyz` 접속 성공
- `docker exec` 실패 확인 — `pullim` 계정이 docker 그룹에 없음 (`groups=1001(pullim)`)
- **psql 직결로 우회.** 포트는 5432가 아니라 **15432** (`docker-compose.yml`이 `127.0.0.1:15432:5432` 매핑)
- `~/.pgpass` 등록 (600). 이제 비밀번호 없이 붙음

**서버 상태 확인**
- PostgreSQL 18.6 · 확장 4종 설치됨 (`vector 0.8.1` · `pg_trgm` · `unaccent` · plpgsql)
- **테이블 0개.** `001_extensions.sql`은 반영됐는데 `002_schema.sql`이 안 돌았음
  → 볼륨이 먼저 생성된 뒤 `002`가 추가된 것으로 보임. init은 최초 기동 때만 실행됨
  → docker를 못 쓰므로 볼륨 재생성 대신 **psql로 직접 적용**하기로 함

**스키마 설계**
- 기존 `002_schema.sql` 9개 테이블 읽고, 부족한 것만 얹는 방향으로 결정
- `sql/003_content.sql` 작성 (143줄) — 테이블 7개 + `case_types` 컬럼 확장 + 뷰 1개

**스키마 적용 + 시드 적재 (서버 반영 완료)**
- `002_schema.sql` → `003_content.sql` 순서로 psql 직접 적용. **테이블 15개**
- `010_seed_case_types.sql` — 유형 16행 (T1~T16)
- `020_seed_questions.sql` — 문항 9행 + 선택지 44행
- `030_seed_type_rules.sql` — 판정 룰 20행
- `900_check.sql`로 점검 — **룰이 없는 유형 0건**, 즉시중단 선택지 7개 확인

**은행 20곳 — 새로 조사하지 않았다. 이미 있었다**
- `기획자료/5. 3자 사기 자료/.../cases/11-bank-requirements.md` 발견
  2026-08-29 Playwright 크롤링 · 20개사 전수 · 출처 URL 포함
- `040_seed_banks.sql`로 적재 — `banks` 20행 + `bank_requirements` 14행
- 요구서류를 보여줄 수 있는 곳은 **5곳뿐** (카카오·씨티·전북·하나·케이뱅크)
- 15곳은 미공개 → 법정 필수 2종으로 대체해야 함
- 조사 문서에 URL 자체가 없던 4곳(IBK·토스·광주·제주)은 `source_url` NULL

**문서**
- `내작업_흐름도.md` 작성 (비개발자용, 용어 풀이 포함)

## 결정한 것

| 무엇 | 어떻게 | 왜 |
|---|---|---|
| 유형 판정 | **룰 테이블** (`type_rules`), LLM 아님 | 재현 가능해야 디버깅됨. T12를 T1로 오판하면 도박죄 자수 문서가 나감 |
| 근거 전달 | `snippet_packs` 키 조회 | 유형이 16개뿐. 검색이 필요 없고 프롬프트 캐시가 살아남 |
| 임베딩 | `document_chunks`에만, 소명서 경로엔 미사용 | 검색 결과가 매번 달라지면 같은 사람에게 다른 소명서가 나옴 |
| 문진 선택지 | **C가 DB에 정의**, A는 렌더링만 | `option_key`가 `type_rules`의 입력값. A가 임의로 정하면 룰이 조용히 실패 |
| 기존 스키마 | 손대지 않고 `003`으로 확장만 | 백엔드가 나중에 붙을 때 충돌 없음 |
| `discloses_documents` 기준 | **보여줄 서류 항목이 하나라도 있으면 true** (5곳) | 하나·케이뱅크는 안내문은 없지만 배포 중인 법정서식에 첨부서류 2항이 적혀 있어 사용자에게 보여줄 게 실제로 있다. 안내문 열거(3곳)와의 차이는 `guidance_note`에 적었다 |

## 막힌 것

- `docker` 그룹 권한 없음 → 볼륨 재생성 불가. psql 직접 적용으로 우회했으므로 당장은 문제 없음
- `002`+`003` 적용은 아직 안 함 (권하진 서버라 확인 후 진행)

## 내일 (9/3)

- ~~`banks` 20곳~~ ✅ 9/2에 끝남 (기존 조사 자료 발견)
- **`case_digests` T1·T4·T12 착수** (유형×5섹션 = 15행) ← 최우선으로 올라옴
- `corpus_documents` 적재 — digest의 출처가 되어야 함
- `statutes` 3~5건 (제2조·제7조)
- `case_types.clause` 채우기 — 유형별 조항 매핑이 아직 비어 있음
