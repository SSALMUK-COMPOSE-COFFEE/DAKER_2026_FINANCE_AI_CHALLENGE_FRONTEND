# 2026-09-03

## 한 일

**T1·T4·T12 세로 뚫기 (원문 → digest → 팩)**
- `050_seed_corpus.sql` — 원문 7건. 루리웹 8,983자 / 디시 9,530자 / 로톡 5건
- `060_seed_digests.sql` — digest 15행 (3유형 × 5섹션)
- `100_build_packs.sql` — 팩 3건 조립. T1 3,053자 / T4 3,371자 / T12 3,173자

**검증 재료 준비**
- `mock/프롬프트/시스템.md` · `mock/사연/` 3건 · `mock/채점표.md`
- `scripts/run_mock.py` — GPTMock(OpenAI 호환) 호출. 표준 라이브러리만 씀

**인수인계 문서**
- `인수인계.md` — 세팅부터 검증 실행까지. 함정 7개 정리

## 결정한 것

| 무엇 | 어떻게 | 왜 |
|---|---|---|
| 검증 범위 | T1·T4·T12 **세 유형만 먼저** | 팩 형식이 미검증이다. 16유형을 다 채운 뒤 형식이 틀리면 전부 다시 써야 한다 |
| T12를 넣은 이유 | 차단 검증용 | 실패하면 팩 문제가 아니라 설계 문제라 빨리 알아야 한다 |
| 범위 밖 유형의 팩 | 미검수 digest도 넣음 | 팩 내용이 소명서 문안이 아니라 **차단 안내문**이라 원문 두께가 필요 없다 |
| GPTMock 용도 | **검증 전용** | 비공식 프록시다. 배포 서버는 compose대로 OpenRouter를 쓴다 |
| SSH 키 | 인계받는 사람은 **본인 키를 따로 발급** | 개인키 공유 금지 |

## 막힌 것

- **GPTMock 미기동.** `uv` 미설치 + OAuth 로그인은 브라우저가 필요해 사람이 직접 해야 함.
  스크립트와 재료는 다 준비됐으므로 로그인 후 `python scripts/run_mock.py` 한 줄이면 됨
- **T12 `verified=false`.** `summary.csv` 스니펫이 201자로 잘려 있음.
  차단 안내문 용도라 당장은 문제 없으나, 두껍게 하려면 로톡 13건 원문 수집 필요
- `statutes` 0건 → 채점 2번(조항 인용) 항목을 아직 제대로 볼 수 없음

## 내일

- GPTMock 로그인 후 검증 1회
- 채점 결과에 따라 팩 형식 확정 또는 수정
- `statutes` 제2조·제7조 입력
- 통과 시 나머지 13개 유형 digest 착수
