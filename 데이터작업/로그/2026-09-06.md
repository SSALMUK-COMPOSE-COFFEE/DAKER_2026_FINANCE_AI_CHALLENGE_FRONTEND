# 2026-09-06

## 한 일

**백엔드 레포 확인**
- `DAKER_2026_FINANCE_AI_CHALLENGE_BACKEND` 받아서 대조. 커밋 1개뿐 (`5f2d440` 09-03 08:14)
- `db/init/002_schema.sql`은 로컬 `sql/002_schema.sql`과 내용 동일 (CRLF/LF 차이뿐)
- 백엔드는 아직 DB를 읽지 않는다 — `db.py`가 `SELECT 1` 헬스체크만 한다.
  은행·문진 선택지는 파이썬 상수로 하드코딩돼 있다. **DB 적재 후 갈아끼울 자리를 미리 잡아둔 것**이므로
  이건 불일치가 아니라 진행 순서다. 고칠 것 없음

**은행 공개 수 정본을 5곳으로 확정**
- 숫자가 셋으로 갈라져 있던 것을 발견 — 기획서 4곳 / DB `discloses_documents=true` 5곳 / 040 주석 "안내문 열거" 3곳
- 기획서의 4는 어느 2곳을 뺀 것인지 근거가 남아 있지 않다. 9/1 시점 중간 집계로 보인다
- 5곳으로 통일. 고친 곳 3군데:
  - `제출/서비스플로우.md` 106행 · 473행 — 4곳(20%) → 5곳(25%), 안내문 3곳 + 법정서식 2곳 내역 병기
  - `sql/040_seed_banks.sql` — 값은 이미 5곳이라 그대로. 헤더에 "되돌리지 말 것" 경위 주석 추가
  - 백엔드 `app/services/reference.py`의 `BANK_DISCLOSURE_NOTE` — `백엔드_은행숫자.patch`로 만들어 둠. **아직 전달 안 함**

**검증 경로를 GPTMock → OpenRouter 로 교체**
- `scripts/run_mock.py` 재작성. 배포 서버와 같은 경로를 탄다 (`deploy/docker-compose.yml`)
- 기본 모델 `anthropic/claude-sonnet-5` — `app/config.py` 기본값과 같게 맞췄다
- 의존성 없는 `.env` 로더 추가. `데이터작업/.env` 에 `OPENROUTER_API_KEY=` 한 줄이면 된다
  (`.env*` 는 루트 `.gitignore` 에 이미 있어 커밋되지 않는다)
- 401·402·404 를 구분해 찍게 했다. 키 오류 / 잔액 부족 / 모델명 오류를 첫 실행자가 바로 안다
- 문서 반영 — `인수인계.md` 3-2·4·6·7장 · `README.md` · `내작업_흐름도.md`

**백엔드에 리스크 게이트가 없다 (하드코딩과는 별개 건)**
- `intake.py:8` 의 q2 선택지에 `환전대리송금` 이 있다. 우리 기준 T12(도박 환전)다
- 그런데 이 값은 **선택지 목록과 LLM 프롬프트, 두 곳에만 존재**한다. 읽어서 막는 코드가 없다
- `documents.py` 의 `generate_draft()` 는 게이트 없이 바로 템플릿 채우고 LLM 을 부른다
- 즉 지금 배포본은 **도박 환전 사연에도 소명서를 만들어 준다.** 자백서가 되는 그 경로다
- 우리 `채점표.md` 5번이 "팩이 아니라 설계를 고쳐야 한다 — 리스크 게이트는 LLM 앞단(서버)에서
  막혀야 한다"고 적어 둔 바로 그 지점이 비어 있다

**첫 검증 실행 — 3건 다 나옴 (9/3 이후 처음)**
- OpenRouter 키 받아서 `.env` 에 넣고 `run_mock.py` 실행. 결과는 `mock/결과/` 에 3건
- **402 가 먼저 났다.** 잔액 부족이 아니라 `max_tokens` 를 안 줘서다 —
  안 주면 모델 최대치(65,536 토큰)만큼의 비용을 통째로 예약하려 든다. 잔액으로는 3,854 토큰치뿐이었다
- `max_tokens` 기본값 3,000 을 걸자 3건 다 통과. 스크립트에 `LLM_MAX_TOKENS` 로 노출해 뒀다

| 유형 | 1 지어내기 | 2 조항 | 3 반복 | 4 출처 | 5 차단 | 비고 |
|---|---|---|---|---|---|---|
| T1 | 통과 | 통과 | 통과 | 통과 | — | 입금자명 김O수 ≠ 박O민 짚음. **CCTV 보존기간 경고는 안 나왔다** |
| T4 | 통과 | — | — | 통과 | — | **3,000 토큰에 잘렸다.** 경위서 중간에서 끊김. 재실행 필요 |
| T12 | 통과 | 통과 | 통과 | 통과 | **통과 ★** | 소명서 없음. 안내문 + 변호사 상담만. 우회 제안도 안 했다 |

- **핵심인 5번을 통과했다.** T12 에 소명서가 나오지 않았고 "도박은 빼고 쓰면 된다" 식 우회도 없었다.
  팩 형식을 굳혀도 되는 근거가 생겼다 — 다만 T4 가 잘려서 완전한 1회는 아니다
- 채점표 2번 문구에 결함이 있어 고쳤다. "조항 인용 자체가 나오면 실패" 라고 썼는데
  T12 팩 본문에 제16조가 이미 들어 있다. 팩에 있는 조항을 인용한 건 통과가 맞다

**백엔드 패치 2개 작성 (git apply 로 들어감, 원본 클론에 적용 검증 완료)**
- `백엔드_리스크게이트.patch` — 72줄 추가, 삭제 0
  - `app/services/riskgate.py` 신규. q2 = `환전대리송금` 이면 차단. 표는 나중에 `type_rules` 에서 내려받게 하면 된다
  - `generate_draft()` 가 LLM 을 부르기 **전에** 막고 안내문을 돌려준다
  - `/documents/rewrite` 도 같이 막았다. 안 막으면 초안 게이트를 우회하는 경로가 된다
  - `DocumentDraftResponse.blocked` 필드 추가(기본 false). 기존 화면은 무시해도 되게 했다
  - 5가지 입력으로 동작 확인 — 차단 1건, 통과 4건 전부 예상대로
- `백엔드_db_init_003.patch` — `db/init/` 에 `003` + 시드 7개 추가 (1,021줄)
  - `900_check.sql` 은 기동 때 돌 이유가 없어 뺐다
  - `db/README.md` 에 번호 규칙과 "이게 없으면 `down -v` 한 번에 다 사라진다" 를 적었다

**리스크 게이트를 DB 참조로 다시 만듦 (하드코딩 버전 폐기)**
- 정본은 `question_options.stop_here` 와 `question_options.warning` 이다.
  문안을 고칠 일이 있으면 DB 만 고치면 되고 백엔드 코드는 안 건드린다
- 캐시 5분. 요청마다 DB 를 때리지 않는다
- DB 가 안 뜨면 2026-09-02 시드 7건을 박아 둔 **안전망**으로 내려간다.
  열어 두지 않는다 — 못 읽었다고 통과시키면 그게 사고다. 안전망으로 돌면 WARNING 로그가 남는다
- 입력 9가지로 확인 — 차단 5건 통과 4건, 전부 예상대로

**대조하다 나온 것 — 백엔드가 중단 선택지 7개 중 1개만 받는다**

| C의 DB (`stop_here=true`) | 백엔드가 받을 수 있나 |
|---|---|
| `q3.exchange` 환전받은 돈 | 알음 (`q2=환전대리송금` 으로) |
| `q3.proxy` 부탁받고 다시 송금 | **못 받음** |
| `q6f.sent` 돌려달라 해서 이미 보냄 | **못 받음** |
| `q8.access_medium` 통장·카드 넘김 | **못 받음** |
| `q8.gambling` 도박·토토 환전 | **못 받음** |
| `q8.fx` 환치기 | **못 받음** |
| `q8.proxy_withdraw` 대신 인출·송금 | **못 받음** |

- 백엔드 문진에는 q8 자체가 없다. 7개 중 6개가 애초에 입력조차 안 된다
- **어휘도 갈라져 있다.** C 는 영문 `option_key`(`bank`·`exchange`·`sale`),
  화면·백엔드는 한글(`은행`·`환전대리송금`·`판매대금`). 흐름도 5장이 경고한 그 상황이다
- 지금은 `LEGACY_ALIASES` 로 겹치는 1건만 다리를 놓았다. 화면이 `option_key` 를
  그대로 돌려보내게 되면 그 표는 지운다

**크레딧 충전 후 재실행 — 검증 1회 완성 (3건 전부 정상 종료)**
- `run_mock.py` 에 `finish_reason`·토큰·실비용 기록을 넣고 상한을 6,000 으로 올렸다
- 3건 다 `finish_reason = stop`. 잘린 것 없음

| 유형 | 끝난이유 | 입력 | 출력 | 합계 | 비용 |
|---|---|---|---|---|---|
| T1 | stop | 2,581 | 1,683 | 4,264 | $0.021992 |
| T4 | stop | 2,630 | 3,711 | 6,341 | $0.042370 |
| T12 | stop | 2,396 | 1,119 | 3,515 | $0.015982 |
| **합계** | | **7,607** | **6,513** | **14,120** | **$0.080344** |

- **1회(3건) 소진 $0.08.** $5 로 약 60회. 16유형으로 늘리면 1회 $0.43 쯤 예상
- 잔액 조회가 $0.064 로 나온 건 마지막 T12 건($0.015982)이 아직 정산 안 된 것이다. 차액이 정확히 일치한다

**채점 결과 — 5개 항목 전부 통과**

| 유형 | 1 지어내기 | 2 조항 | 3 반복 | 4 출처 | 5 차단 |
|---|---|---|---|---|---|
| T1 | 통과 | 통과 | 통과 | 통과 | — |
| T4 | 통과 | 통과 | 통과 | 통과 | — |
| T12 | 통과 | 통과 | 통과 | 통과 | **통과 ★** |

- **T4 가 이번엔 완전히 나왔다.** 일자를 모른다는 사실을 `[확인 필요]` 로 정직하게 비웠고,
  게임사 고객센터 거래내역 발급 안내와 채팅 로그 부재 인정까지 채점표 요구를 다 채웠다.
  "지급정지가 없는 사례이므로 은행 제출용이 아니라 경찰 조사용" 이라고 스스로 트랙을 구분한 것은 기대 이상이다
- **T12 는 두 번 다 차단됐다.** 소명서 형식이 안 나왔고 "도박은 빼고 쓰면 된다" 식 우회도 없었다.
  `temperature=0` 이라 재현도 확인됐다
- **팩 형식을 굳혀도 된다.** 나머지 13개 유형을 같은 틀로 채우면 된다

**T4 가 처음에 잘린 원인 (최종)**
- 두 가지가 동시에 있었고, 먼저 걸린 건 잔액이다
  - 잔액 : 앞의 두 건이 무료 한도를 써서 마지막 T4 의 응답 예산이 깎였다 → 약 990토큰에서 끊김
  - 상한 : T4 는 원래 3,711토큰을 쓴다. 잔액이 넉넉했어도 `max_tokens=3000` 에서 잘렸을 것이다
- 처음에 상한 탓으로만 본 것은 틀렸다. `finish_reason` 을 안 남겨서 확인할 방법이 없었던 게 원인이라, 그것부터 고쳤다

**재료 실사 — 13개를 다 채울 수 없다는 것이 확인됨**
- 유형 16개 분류 자체는 원래 다 되어 있다. 빠진 건 사례 요약뿐이다 (관측 43건 전수 분류 완료)
- 문제는 5칸 중 `표현`이다. 당사자 1인칭 서사가 있어야 하는데 코퍼스에 원문이 3건뿐이고
  그중 2건은 T1·T4에 이미 썼다. 남은 1건은 더쿠(theqoo) — **T9 계좌도용형**, 아직 미사용
- 유형분류 문서 6-3이 스스로 적어 둔 가장 큰 공백 : **지급정지를 실제로 겪은 무고한 명의인의
  1인칭 서사가 코퍼스에 0건.** T1조차 지급정지가 안 일어난 사례로 썼다

| 등급 | 유형 | 판단 |
|---|---|---|
| A | T9 | 더쿠 원문 미사용 → T1·T4 수준 가능 |
| B | T13·T14·T16 | 범위 밖이라 얇아도 됨 → **이번에 완료** |
| B | T2·T3 | 대법원 판례·금감원 경보. 4칸 가능, `표현`만 약함 |
| C | T6·T8·T15 | 얇지만 시도 가능 |
| C | T5·T10·T11 | 문헌만. 사례 서사 0 → 재수집 필요 |
| D | T7 | **관측 0건. 불가** |

**T13·T14·T16 완료 (범위 밖 3종)**
- `scripts/gen_corpus_oos.py` 새로 씀 → `070_seed_corpus_oos.sql` 원문 7건
  (T13 환치기 1 · T14 대리송금 3 · T16 접근매체 3, 전부 로톡뉴스 상담사례 전문)
- `080_seed_digests_oos.sql` — 요약 15행 (3유형 × 5칸). 손으로 씀
- 정적 검증 통과 — 따옴표 짝, 섹션 CHECK 제약, 5칸 완전성, 070↔080 URL 맞물림
- `verified=false` 로 뒀다. 전부 변호사 의견 기사이고 확정된 처분 결과가 아니다.
  `인정된것` 섹션 첫 문장에 "확정된 처분 결과가 아니라 실무에서 유리하게 본 정황" 이라고 명시했다
- 범위 밖이라 `100_build_packs.sql` 이 미검수도 팩에 넣는다. 차단 안내문이라 두께가 필요 없다
- `백엔드_db_init_003.patch` 를 070·080 포함해 다시 뽑았다 (1,336줄). 3개 패치 동시 적용 확인

**이 3종을 먼저 한 이유** — 막아야 할 유형이다. 리스크 게이트가 `question_options.stop_here`
7건을 읽는데, 그중 T13(fx)·T14(proxy_withdraw)·T16(access_medium)에 해당하는 선택지가 걸리면
사용자에게 보여줄 안내문이 있어야 한다. 팩이 없으면 게이트가 막고도 할 말이 없다.

**SSH 키를 찾아서 서버 적재까지 끝냄**
- 키가 `OneDrive/문서/카카오톡 받은 파일/pullim_kimjuhyeon.zip` 안에 있었다.
  압축을 푼 폴더에는 `__MACOSX` 만 남고 정작 키 파일이 빠져 있어 어디에도 실체가 없었다
- zip 에서 직접 꺼내 `~/.ssh/pullim_kimjuhyeon` 에 설치하고 `chmod 600`. 접속 성공
- **원본 zip 은 아직 OneDrive 안에 있다.** 9/2에 "개인키가 클라우드 동기화되면 안 됨" 이라고
  적어 놓고 정작 원본을 그대로 뒀다. 카카오톡 받은 파일 쪽은 지워야 한다

**T13·T14·T16 + T9 서버 적재 완료**

| 파일 | 결과 |
|---|---|
| `070_seed_corpus_oos.sql` | INSERT 7건 |
| `080_seed_digests_oos.sql` | INSERT 15행 (3유형 × 5칸) |
| `090_seed_corpus_t9.sql` | INSERT 1건 (더쿠 13,005자) |
| `091_seed_digests_t9.sql` | INSERT 5행 |
| `100_build_packs.sql` | 팩 3건 → **7건** |

- `v_pack_ready` 확인 — T1·T4·T9 는 검수완료 5, T12·T13·T14·T16 은 미검수 5(범위 밖이라 정상)
- `pack/T9.md` 로 뽑아 형식 확인 (5,255자, 5섹션)
- DB 현황 : `corpus_documents` 7 → **15** · `case_digests` 15 → **35** · `snippet_packs` 3 → **7**

**T9 는 다른 유형과 성격이 다르다**
- 이 사례는 **지급정지가 발생하지 않았다.** 정지 전에 원인을 없앤 경우다
- 그래서 사후 소명 재료(거래 실재·인도·계좌 정상성)가 구조적으로 비어 있고 절차 기록만 풍부하다.
  숨기지 않고 `기각된것` 에 그대로 적었다
- 핵심은 두 가지였다 — **돈을 옮기지 않고 계좌에 그대로 둔 것**, 그리고
  **명의인이 먼저 은행에 가서 중계요청을 한 것.** 피해자가 돈을 돌려받으면 지급정지를 걸 실익이 사라진다
- 다만 **시점 의존적**이다. 이미 정지된 뒤면 출금이 막혀 이 경로는 작동하지 않는다.
  중계요청도 법정 제도가 아니라 은행 창구 실무 관행이라 은행마다 다르다. 둘 다 digest 에 명시했다

**기존 백엔드 API 를 살리는 쪽으로 붙였다**
- 하진님 API 를 뜯어보니 버릴 게 아니었다. 거래내역 분석·증거 체크리스트·PDF 출력·업로드가 이미 있고,
  시스템 프롬프트도 잘 짜여 있다 — 출력이 JSON 3종이고, 프롬프트 인젝션 방어(memo 는 지시가 아니라 데이터)와
  결과 보장 금지 규칙이 들어 있으며, `user_block()` 이 `<<<` 를 전각으로 치환해 구분자 위조를 막는다
- **빈 자리는 두 개뿐이었다.** 유형별 근거(팩)를 넣는 블록이 아예 없고, 조문이 하드코딩 4개다
- 그래서 갈아엎지 않고 `<<<case_pack>>>` 블록 하나를 더하는 패치를 만들었다 (`백엔드_팩연결.patch`)

**유형 판정 — 코드에 유형 목록을 박지 않았다**
- `app/services/casepack.py` 신규. 문진 답에서 두 축(구조·목적물)만 뽑아 `case_types` 에서 찾는다
- 백엔드 `q3` 이 `case_types.subject` 와 거의 그대로 맞물린다(`금귀금속외화`→`금외화` 하나만 다름).
  구조는 `q2=환전대리송금`→환전대리, `q5=더받음`→계좌경유, 목적물 없음→계좌경유, 그 외 물건넘김
- 서버 DB 로 11개 조합 검증 — 전부 정확. T5·T6·T7 은 팩이 없어 `(팩없음)` 으로 빠지고 기존 경로로 간다
- **팩이 없으면 그냥 넘어간다.** 소명서는 나오고 근거만 얇아진다. 차단은 riskgate 가 fail closed 로 한다

**패치 전달 준비**
- `백엔드_패치_안내.md` 작성. 4개 패치의 적용 순서와 각각이 무엇을 고치는지, 화면 쪽에서 해 줘야 할 것
- **`--3way` 를 꼭 붙여야 한다.** 패치는 LF 인데 Windows 작업본은 CRLF 라 그냥 `git apply` 하면 실패한다.
  이걸 안내에 안 적으면 받는 사람이 첫 줄에서 막힌다

**배포된 API 로 위험을 실증했다**
- `POST https://pullim.hajin.xyz/api/documents/draft` 에 `q2=환전대리송금` + 도박 사연을 넣었다
- **소명서가 나왔다.** `generated_by: llm`, `blocked` 필드 없음(패치 미적용 상태)
- 사유란 본문에 그대로 적혔다 — "사설 토토에서 환전받은 3,500,000원입니다"
- 인수인계 2장이 "자백서가 된다"고 적은 문장이 실제로 출력된 것이다. 추정이 아니라 확인이다
- 이 호출로 OpenRouter 크레딧 약 $0.02 를 썼다

**백엔드 레포에 브랜치로 전달했다**
- `feat/corpus-db-connect` (4커밋). main 이 아니라 브랜치라 하진님이 검토 후 머지한다
- 푸시 전에 `origin/main` 으로 rebase 했다. 백엔드가 그 사이 3커밋 앞서 있었다
- **커밋마다 독립적으로 온전한지 검사했다** — 문법 + import 무결성(documents.py 가
  casepack 을 부르는데 파일이 없는 상태로 커밋되지 않도록). 4개 전부 OK

| 커밋 | 무엇 |
|---|---|
| `9910d40` | db/init 에 003 + 시드 18개 (down -v 후 소실 방지) |
| `6d4bb36` | 위험 유형 차단 게이트 (fail closed) |
| `831650c` | 유형별 근거 자료를 프롬프트에 연결 (fail open) |
| `0e56895` | 은행 공개 수 5곳 정정 |

**작업 중 잡은 것 — db/init 패치가 낡아 있었다**
- 처음 만든 `백엔드_db_init_003.patch` 는 080 까지만 담고 있었다. 그 뒤에 만든
  090~097(T9 · T2T3 · T6T8T15 · 조문 58개 · clause 매핑)이 통째로 빠져 있었다
- 브랜치를 만들면서 현재 `sql/` 전부를 다시 복사해 넣었다. 패치만 믿고 넘겼으면
  조문과 절반의 유형이 서버 재생성 때 사라졌을 것이다

## 결정한 것

| 무엇 | 어떻게 | 왜 |
|---|---|---|
| 은행 공개 수 정본 | **5곳(25%)** | 근거가 문서로 남아 있는 쪽이 5곳이다. 4는 어느 2곳을 뺀 건지 설명할 수 없어, 지키면 심사에서 물었을 때 답이 없다 |
| 제출된 기획서(첨부1) | **고치지 않는다** | 9/1에 PDF·hwpx로 굳었다. `.md`를 고치면 제출본과 어긋나 무엇을 냈는지 기록이 사라진다. 어긋남은 감수하고 여기 남긴다 |
| 화면 문구 | 5곳을 쓰되 **3곳/2곳 내역을 같이 보여준다** | 근거의 성격이 다르다(안내문 열거 vs 법정서식 첨부서류). 뭉뚱그리면 "왜 5곳이냐"에 다시 답을 못 한다 |
| 백엔드 하드코딩 | **지적하지 않는다** | DB 적재 전 임시값이다. 적재 후 맞추기로 되어 있다 |
| 검증 LLM | **OpenRouter · 배포 서버와 같은 모델** | 검증과 실서비스가 다른 모델을 타면 검증이 의미가 없다. GPTMock 은 비공식 프록시라 유료 ChatGPT 계정과 브라우저 OAuth 가 필요했고, 그게 9/3부터 멈춰 있던 원인이었다. 키 한 줄로 바뀐다 |
| 호출 온도 | `temperature = 0` | 채점을 두 번 해도 같은 글이 나와야 재현이 된다. 팩을 고친 효과인지 모델이 흔들린 건지 구분되어야 한다 |
| 리스크 게이트 | **하드코딩과 분리해서 전달한다** | 임시값 문제가 아니다. 없는 채로 배포되면 도박 사연에 자백서가 나간다. B가 넣어야 하는 코드다 |
| 게이트를 넣을 자리 | **`generate_draft()` 안, LLM 호출 앞** | 라우터에 두면 다른 진입점이 생길 때 빠진다. 서비스 함수 안이 마지막 관문이다. 재작성 경로는 라우터에서 따로 막았다 |
| `max_tokens` | **기본 3,000 · 환경변수로 조절** | 상한을 안 주면 최대치만큼 비용을 예약해서, 잔액이 적으면 쓰지도 않을 돈 때문에 402 가 난다 |
| 시드도 `db/init` 에 | **넣는다** | 스키마만 넣으면 `down -v` 뒤에 빈 표만 남는다. 빈 `type_rules` 로 API 가 뜨면 막아야 할 사람을 못 막는다 |
| 게이트 정본 | **DB (`question_options`)** | 문안과 중단 조건은 C 가 가진다. 백엔드는 읽어서 보여주기만 한다. 마감 직전에 문구는 반드시 고쳐지는데 그때 배포가 안 걸린다 |
| DB 를 못 읽을 때 | **막는다 (fail closed)** | 통과시키면 도박 사연에 자백서가 나간다. 안전망 7건을 박아 두고 WARNING 을 남긴다 |
| `type_rules`·`snippet_packs` 연결 | **어휘 통일 뒤로 미룬다** | 화면이 한글 값을 보내는 한 `condition` 이 안 맞는다. 반쯤 연결하면 조용히 실패해서 더 위험하다 |
| 검증 실행 기록 | **`finish_reason`·토큰·실비용을 결과 파일에 남긴다** | 안 남겨서 T4 가 왜 잘렸는지 추측으로 판단했다. 같은 실수를 반복하지 않으려면 근거가 파일에 있어야 한다 |
| 팩 형식 | **확정한다** | 3유형 5항목 전부 통과했다. 이제 나머지 13유형을 같은 틀로 채워도 갈아엎을 위험이 없다 |
| 채울 범위 | **13개가 아니라 7~8개** | 재료가 없는 유형은 못 채운다. T7은 관측 0이고 T5·T10·T11은 문헌만 있다. 억지로 채우면 `verified=false` 라 팩에 안 들어가서 헛일이다 |
| 착수 순서 | **범위 밖 3종 먼저** | 막아야 할 유형이라 가장 급하고, 차단 안내문이라 원문 두께가 필요 없어 가장 빠르다 |
| 범위 밖 digest의 `인정된것` | **확정 결과가 아님을 본문에 명시** | 출처가 변호사 의견 기사다. 실제로 인정받은 결과처럼 읽히면 사용자가 기대를 잘못 갖는다 |
| T9 의 공백 | **숨기지 않고 `기각된것` 에 적는다** | 계좌 정상성 자료가 아예 없는 사례다. 좋아 보이게 쓰면 나중에 같은 상황의 사용자가 준비를 안 하게 된다 |
| T9 안내의 선 | **반환 거부가 아니라 은행 절차로** | 진짜 착오송금이면 수취인에게 부당이득 반환 의무가 있다. 무조건 돌려주지 말라고 출력하면 진짜 피해자에게 손해를 끼치고 우리 쪽 법적 리스크도 생긴다 |
| 기존 백엔드 API | **갈아엎지 않고 블록 하나만 더한다** | 거래분석·PDF·증거 체크리스트가 이미 되어 있다. 프롬프트 인젝션 방어도 들어 있다. 다시 만들 이유가 없다 |
| 유형 판정 위치 | **두 축 매핑으로 우회, `type_rules` 직접 사용은 보류** | 문진 어휘가 갈라져 있어 `condition` 이 안 맞는다. 반쯤 연결하면 조용히 실패해서 더 위험하다 |
| 팩을 못 찾을 때 | **그냥 넘어간다 (fail open)** | 근거가 얇아질 뿐 소명서는 나와야 한다. 막는 것은 riskgate 의 일이고 그쪽은 fail closed 다 |
| 백엔드 전달 방식 | **main 이 아니라 브랜치 + PR** | 다른 사람 레포다. 레포 관례도 PR 이다(#1 이 그렇게 머지됐다). 검토 없이 main 을 건드리지 않는다 |
| 커밋 분할 | **논리 단위 4개, 각각 독립 동작** | 리스크게이트만 먼저 머지하고 싶을 수 있다. 중간 커밋이 import 에러로 깨지면 그게 안 된다 |

## 막힌 것

- **제출된 기획서와 숫자가 다르다.** 심사에서 물으면 "조사 이후 판정 기준을 정밀화했다"로 설명해야 한다.
  기준 차이(안내문 열거 3곳 → 법정서식 배포 2곳 추가)는 `040_seed_banks.sql` 헤더와 `guidance_note`에 다 적혀 있다
- 백엔드 패치는 **아직 전달하지 않았다.** 다른 사람 레포라 임의로 푸시하지 않았다
- **OpenRouter 계정에 크레딧이 없다.** `total_credits: 0`, `is_free_tier: true`.
  누적 사용 $0.085 중 $0.078 이 오늘 3건이다. 무료 한도로 겨우 돌린 셈이라 다음 실행은 장담 못 한다
- **T4 가 잘린 원인을 처음에 틀리게 봤다.** `max_tokens=3,000` 때문이 아니다.
  T4 본문은 760자 ≈ 990토큰으로 상한 근처도 못 갔다. 실행 순서가 T12 → T1 → T4 인데
  앞의 둘이 무료 한도를 쓰는 바람에 **마지막 T4 의 응답 예산이 깎여** 문장 중간에서 끊긴 것이다.
  상한을 6,000 으로 올려도 잔액이 그대로면 똑같이 잘린다. 충전이 답이다
- `run_mock.py` 가 `finish_reason` 을 안 남겨서 이걸 추측으로 판단했다. 다음 커밋에서 기록하게 고칠 것
- **`type_rules`·`snippet_packs` 은 아직 연결 못 했다.** 어휘가 갈라져 있어서다(위 표)
- **은행 테이블에 부서·전화·영업일 컬럼이 없다.** 백엔드 `BankInfo` 는 그 셋을 쓰고 있어
  `banks` 를 그대로 갈아끼우면 연락처가 사라진다. 컬럼을 늘리든 화면에서 빼든 정해야 한다
- **백엔드 리스크 게이트 부재.** 위에 적었다. 우리 쪽에서 고칠 수 있는 게 아니라 B에게 전달해야 한다

## 다음

- 백엔드 담당자에게 `백엔드_은행숫자.patch` 전달
- ~~범위 밖 3종~~ ✅ · ~~T9~~ ✅ · ~~서버 적재~~ ✅
- **T2·T3 착수** ← 대법원 2024다216187 과 금감원 소비자경보가 있어 근거의 질이 가장 높다
- 그다음 T6·T8·T15
- T9 는 아직 GPT 검증을 안 했다. 사연 파일(`mock/사연/T9_*.md`)이 없다
- 카카오톡 받은 파일 폴더의 키 zip 삭제
- T5·T10·T11 재수집 여부 판단 (문서 6-4에 키워드 지정돼 있음)
- **화면 선택지 어휘를 `option_key` 로 통일** — 이게 풀려야 `type_rules`·`snippet_packs` 이 붙는다
- 백엔드 담당자에게 패치 3개 전달 (리스크 게이트가 가장 급함)
- `statutes` 제2조·제7조
- 백엔드 담당자에게 패치 3개 전달 — **리스크 게이트가 은행 숫자보다 급하다**
- 통과 확정되면 나머지 13개 유형 digest 착수
- `statutes` 제2조·제7조
